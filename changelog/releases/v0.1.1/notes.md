This release makes pi-service-tier easier to find by mentioning fast mode in package and repository metadata. Users searching for the /fast command or fast-tier controls can now discover the extension more reliably.

## 🐞 Bug fixes

### Fast mode metadata discoverability

Package and repository metadata now mention `fast mode`, making the extension easier to find when searching for the `/fast` command or fast-tier controls.

*By @mavam and @codex.*
